---
- name: Install apache apache2 et configure la page d acceuil  
  hosts: all
  
  tasks:
  - name: install apache2 
    ansible.builtin.apt:
       name: apache2
       state: present
       update_cache: yes

